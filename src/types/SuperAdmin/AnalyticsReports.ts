export type Performance = {
  id: string;
  company: string;
  subTitle: string;
  revenue: string;
  rate: string;
};
export type performanceData = {
    id: number,
    model: string,
    requests: number,
    accuracy: number,
    avgResponse: string,
    costEfficiency: string,
};
